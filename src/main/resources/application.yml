server:
  port: 8080

spring:
  ai:
    openai:
      base-url: https://llm.t1v.scibox.tech
      api-key: sk-v7-pQZBVeDdViRVr1Hknng
      chat:
        options:
          model: qwen3-32b-awq
          temperature: 0.3

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/interviewer
    username: interviewer
    password: interviewer
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org.springframework.ai: INFO
    com.t1impulse.interviewer: DEBUG