server:
  port: 8080

app:
  admin:
    username: ${ADMIN_USERNAME:admin}
    password: ${ADMIN_PASSWORD:admin123}
  base-url: ${BASE_URL:http://localhost:8080}
  system:
    token: ${SYSTEM_TOKEN:qwerty}

spring:
  ai:
    openai:
      base-url: https://llm.t1v.scibox.tech
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: qwen3-32b-awq
          temperature: 0.3

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/interviewer
    username: interviewer
    password: interviewer
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    jwt:
      secret: ${JWT_SECRET:dGhpc2lzYXZlcnlsb25nc2VjcmV0a2V5Zm9yand0dG9rZW5zMTIzNDU2Nzg5MA==}

logging:
  level:
    org.springframework.ai: INFO
    com.t1impulse.interviewer: DEBUG