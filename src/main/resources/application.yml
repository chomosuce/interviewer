server:
  port: 8080


spring:
  application:
    name: interviewer
  ai:
    openai:
      base-url: https://llm.t1v.scibox.tech
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: qwen3-32b-awq
          temperature: 0.3

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/interviewer
    username: interviewer
    password: interviewer
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    jwt:
      secret: superduperpupersecretkeyforsigningjwt1234567890

logging:
  level:
    org.springframework.ai: INFO
    com.t1impulse.interviewer: DEBUG